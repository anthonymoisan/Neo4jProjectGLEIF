Les fichiers XML n'ont pas été importés sous GIT HUB de part la volumétrie. Pour la base monde, on est à plus de 2 Go.

Il suffit 
1) de les télécharger sur le site de https://www.gleif.org/fr/lei-data/gleif-concatenated-file/download-the-concatenated-file
2) de décompresser les 2 fichiers dans le répertoire XML. 
3) renommer les fichiers niveau1.xml et niveau2.xml
4) utiliser le convertisseur à travers le notebook jupyter qui utilisera le code Python de src/utils et va créer les deux fichiers csv dans data